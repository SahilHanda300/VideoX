const postLogin = require("./postLogin");
const postRegister = require("./postRegister"); // Handles user registration

exports.controllers = {
  postLogin,
  postRegister,
};


exports.controllers = {
  postLogin,
  postRegister,
};
